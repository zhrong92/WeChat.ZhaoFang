function indexOf(arr, value) {
  var isExist = false;
  for(var i = 0; i < arr.length; i++) {
    if (arr[i] == value) {
      isExist = true;
    }
  }
  return isExist;
}

module.exports.indexOf = indexOf;